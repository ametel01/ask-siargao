# Implementation Plans

Generated by the improve skill on 2026-06-23. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Keep Drizzle schema in lockstep with the SQL migration | P1 | M | - | DONE |
| 002 | Load checkout eligibility from persisted server state | P1 | M | 001 | DONE |
| 003 | Apply verified Stripe webhooks to the audit lifecycle | P1 | M | 002 | DONE |
| 004 | Gate report rendering on payment, publication, and access | P1 | M | 003 | DONE |
| 005 | Keep retrieval evidence IDs scoped to selected facts | P1 | M | - | DONE |
| 006 | Derive public eligibility from governed source policy | P1 | M | 001 | DONE |
| 007 | Automate the release gate in CI | P1 | M | - | DONE |
| 008 | Allow route-only intake when origin is absent | P2 | S | - | DONE |
| 009 | Back production rate limits with shared trusted storage | P2 | M | - | DONE |
| 010 | Build the first real provider ingestion slice | P2 | L | 001, 006 | DONE |
| 011 | Generate public pages from persisted governed facts | P2 | M/L | 001, 006 | DONE |
| 012 | Require every mandatory report category in validation | P1 | S/M | - | TODO |
| 013 | Compute evidence freshness against the current time | P1 | S | 005 | TODO |
| 014 | Return stable JSON errors for malformed intake requests | P2 | S | 002 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency Notes

- 001 precedes 002, 006, 010, and 011 because persistence work is harder and less type-safe while migrated tables are missing from `src/server/db/schema.ts`.
- 002 precedes 003 because webhook handling must look up the pending checkout/payment created by checkout.
- 003 precedes 004 because report access needs a real paid/published lifecycle state to verify.
- 006 precedes 010 and 011 because provider ingestion and persisted public pages must not bypass source permissions.
- 005 is independent and should land early because it protects the evidence/citation promise used by generated reports.
- 013 should run after 005 because both modify `src/server/audit/evidence-bundles.ts`; 005 establishes evidence-to-fact scoping, then 013 fixes freshness labels on that bundle.
- 014 should run after 002 because checkout malformed-JSON handling is already part of the checkout route hardening plan; 014 completes the same stable-error boundary for intake without duplicating checkout ownership.
- 012 can run independently and should land before relying on generated reports for release-candidate review because it tightens deterministic report validation.

## Findings Considered And Rejected

- Next/Biome major-version migration: not planned in this batch because it is lower leverage than payment lifecycle, public/source governance, and CI. Revisit after core production boundaries are durable.
- Public index API pagination/cache headers: not planned in this batch because the corpus is currently five fixture pages. Revisit when public pages are generated from persisted facts.
- Local onboarding PGlite docs clarity: not planned separately because the persistence plans will force a clearer runtime database story.
- Mutating `bun run format` in docs: not planned separately because plan 007 should add a non-mutating CI format/lint gate and update docs as part of that work.
